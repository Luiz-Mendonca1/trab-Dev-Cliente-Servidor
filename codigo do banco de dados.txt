CREATE DATABASE estoque;
USE estoque;

CREATE TABLE produtos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    preco DECIMAL(10, 2) NOT NULL CHECK (preco >= 0), 
    quantidade INT NOT NULL CHECK (quantidade >= 0),
    categoria VARCHAR(100) NOT NULL
);

-- Testando a visualização
SELECT * FROM produtos;

-- Inserções válidas (Funcionam normalmente)
INSERT INTO produtos (nome, preco, quantidade, categoria) VALUES ('Teclado Mecânico',-250.00, -15, 'Periféricos');
INSERT INTO produtos (nome, preco, quantidade, categoria) VALUES ('Mouse Gamer', 120.00, 30, 'Periféricos');
INSERT INTO produtos (nome, preco, quantidade, categoria) VALUES ('Monitor 24p', 850.00, 5, 'Monitores');

-- EXEMPLO DE ERRO: Se você tentar executar a linha abaixo, o banco retornará um erro de restrição (Constraint violation)
-- INSERT INTO produtos (nome, preco, quantidade, categoria) VALUES ('Produto Errado', -10.00, -5, 'Teste');

-- Exemplo de Update válido
UPDATE produtos SET preco = 260.00 WHERE nome = 'Teclado Mecânico';

-- Exemplo de Delete
DELETE FROM produtos WHERE id = 2;
